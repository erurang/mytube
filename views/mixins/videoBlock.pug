mixin videoBlock(video={})
    .videoBlock
        a(href=routes.videoDetail(video.id))
            video.videoBlock__thumbnail(src=video.fileUrl,witdh="320",height="200")
            .videoTop
                img(src=video.avata)
            .videoBot                
                p(class="videoTitle")=video.title
                p(class="videoCreator")=video.creator
                p 조회수 
                    span=video.views
                    |회•
                    span=video.createdAt
                    