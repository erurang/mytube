//- 메인 홈화면 올라온 비디오가 순서대로 올라가있을 계획
extends ./layouts/main.pug

block content
    include ./partials/list.pug
    .user__profile
        .user__profile__header
            if user.avataUrl
                img.avatar(src=user.avataUrl)
            h4.profile_username=user.name
        a(href=routes.yourPost(id)) 작성글보기